{
  "name": "home-in",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "clean:dist": "pnpm -r clean:dist && rimraf dist",
    "clean:all": "pnpm -r clean:all && rimraf --glob dist node_modules",
    "test": "pnpm -r test",
    "build:all": "pnpm -r build:all",
    "bundle:pack": "pnpm -r bundle:pack",
    "bundle:aws": "pnpm -r bundle:aws",
    "bundle:deploy": "pnpm -r bundle:deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@jest/globals": "^29.7.0",
    "@npmcli/promise-spawn": "^7.0.1",
    "@types/archiver": "^6.0.2",
    "@types/aws-lambda": "^8.10.133",
    "@types/jest": "^29.5.12",
    "@types/node": "^18.19.17",
    "@types/npmcli__promise-spawn": "^6.0.3",
    "archiver": "^6.0.1",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
